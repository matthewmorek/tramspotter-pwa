/* eslint-disable no-unused-vars */
import AppInfo from '../AppInfo';

export default {
  title: 'Patterns|AppInfo'
};

export const standard = () => ({
  components: { AppInfo },
  template: '<app-info />'
});
