/* eslint-disable no-unused-vars */
import AppInfo from '../AppInfo';

export default {
  title: 'Patterns|AppInfo'
};

export const Default = () => ({
  components: { AppInfo },
  template: '<app-info />'
});
